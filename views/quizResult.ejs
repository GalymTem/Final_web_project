<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Quiz Results</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-5">
    <h2>🎮 Your Quiz Results</h2>
    <p>You scored <strong><%= score %></strong> out of <strong><%= total %></strong>!</p>

    <% if (score === total) { %>
        <p class="text-success">🎉 Perfect score! You're a gaming genius! 🎮</p>
    <% } else if (score >= total / 2) { %>
        <p class="text-warning">👍 Good job! Keep playing and you'll get even better.</p>
    <% } else { %>
        <p class="text-danger">😔 Better luck next time! Try again.</p>
    <% } %>

    <!-- Кнопка для того чтобы вернуться на главную страницу -->
    <a href="/mainPage" class="btn btn-primary">Back to Home</a>
    
    <!-- Кнопка для повторного прохождения квиза -->
    <a href="/quiz" class="btn btn-secondary">Try Again</a>
</body>
</html>
